package app.ownplay.player.ui.rebuild

internal enum class TvDestination(
    val label: String,
) {
    HOME("Home"),
    LIVE("Live TV"),
    MOVIES("Movies"),
    SERIES("Series"),
    SETTINGS("Settings"),
}
